# Yağız Efe Gökçe — 2309111075

## Role: Architect & Backend Lead

### Responsibilities

- Designed the overall system architecture: FastAPI backend, FalkorDB graph database, React frontend, Railway + Vercel deployment pipeline
- Defined all 14 entity types and their graph relationships (nodes, edges, properties)
- Implemented FalkorDB connection layer (db/connection.py) with index creation on startup
- Built core authentication system: JWT access + refresh tokens, bcrypt password hashing (core/security.py)
- Implemented routers: auth, destinations, activities, itineraries, users
- Wrote the recommendation engine using multi-hop Cypher traversal (services/recommendation.py)
- Implemented shortestPath() query between destinations
- Created Pydantic v2 models for all 14 entities with full validation
- Configured CORS, Railway Variables, Procfile, and railway.json for production deployment
- Led debugging and deployment issues: Railpack build fix, PORT variable, CORS origins configuration
- Seeded 30 Turkish cities with real coordinates, 81 activities, and all graph relationships

### Key Deliverables

- `backend/main.py` — FastAPI app entry point
- `backend/core/` — config, security, dependencies
- `backend/db/` — FalkorDB connection + seed
- `backend/routers/` — auth, destinations, activities, itineraries, users
- `backend/services/recommendation.py` — graph traversal recommendation engine
- `backend/models/` — all 14 Pydantic models
